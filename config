## programs (update for specific OS)

source "./config.linux"

## configuration

hostname=$(hostname -s)
datetime=$(date +%Y%m%d%H%M%S)

OUTDIR=/tmp/${hostname}
PS_DELAY=10

## setup directories
mkdir -p ${OUTDIR}
