## programs (update for specific OS)

source "./config.linux"

## configuration

hostname=$(hostname -s)
datetime=$(date +%Y%m%d%H%M%S)

OUTDIR=/tmp/${hostname}
PS_DELAY=10
HASH_DIRS="/opt /bin /root /sbin /usr /etc"

## setup directories
mkdir -p ${OUTDIR}
